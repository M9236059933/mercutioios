import UIKit
import SwiftUI
import PlaygroundSupport
@testable import MFramework

struct ContentView: View {
    var body: some View {
       Text("Hello")
    }
}

PlaygroundPage.current.setLiveView(ContentView())
